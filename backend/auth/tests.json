{
  "tests": [
    {
      "name": "GET /me без токена — 401",
      "method": "GET",
      "path": "/me",
      "expectedStatus": 401,
      "expectedBody": {"error": "string"},
      "bodyMatcher": "partial"
    },
    {
      "name": "POST /login — неверный пароль",
      "method": "POST",
      "path": "/login",
      "body": {"email": "noone@example.com", "password": "wrong"},
      "expectedStatus": 401,
      "expectedBody": {"error": "string"},
      "bodyMatcher": "partial"
    }
  ]
}